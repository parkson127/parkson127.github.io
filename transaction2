<!DOCTYPE html>
<html lang="ms">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🐱 PAW Extractor</title>
    <script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js">
    </script>
    <style>
        /* ===== RESET & BASE ===== */
        *{box-sizing:border-box;margin:0;padding:0}
        body{font-family:'Segoe UI',Tahoma,Geneva,Verdana,sans-serif;max-width:1200px;margin:0 auto;padding:15px;background:linear-gradient(135deg,#fdf6e3 0%,#f5e6ca 50%,#e8f5e9 100%);min-height:100vh;position:relative;overflow-x:hidden}
        
        .paw-bg{position:fixed;font-size:28px;opacity:.08;pointer-events:none;z-index:0;animation:floatPaw 20s infinite linear}
        @keyframes floatPaw{0%{transform:translateY(100vh) rotate(0deg) scale(.5);opacity:.05}50%{opacity:.12}100%{transform:translateY(-10vh) rotate(360deg) scale(1);opacity:0}}
        .paw-bg:nth-child(1){left:5%;animation-duration:22s}.paw-bg:nth-child(2){left:20%;animation-duration:18s;animation-delay:3s}.paw-bg:nth-child(3){left:40%;animation-duration:25s;animation-delay:5s}.paw-bg:nth-child(4){left:60%;animation-duration:16s;animation-delay:8s}.paw-bg:nth-child(5){left:80%;animation-duration:21s;animation-delay:2s}.paw-bg:nth-child(6){left:90%;animation-duration:19s;animation-delay:10s}
        
        .container{background:rgba(255,255,255,.92);backdrop-filter:blur(8px);padding:20px;border-radius:16px;box-shadow:0 8px 32px rgba(0,0,0,.12);position:relative;z-index:1;border:2px solid rgba(255,183,77,.3)}
        
        .cat-header{display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin-bottom:10px}
        .cat-icon{font-size:40px;animation:bounceCat 2.5s infinite ease-in-out;cursor:pointer;display:inline-block}
        @keyframes bounceCat{0%,100%{transform:translateY(0)}50%{transform:translateY(-10px)}}
        .cat-header h1{color:#d35400;border-bottom:none;padding-bottom:0;font-size:1.6rem;text-shadow:2px 2px 0 #f39c12;flex:1}
        .cat-header .sub{font-size:0.8rem;color:#7f6000;opacity:0.7;font-style:italic}
        
        .cat-container{display:flex;justify-content:space-between;align-items:center;margin-bottom:10px;padding:10px;background:rgba(255,183,77,.15);border-radius:12px;border:2px dashed rgba(255,183,77,.3);min-height:80px;position:relative;overflow:hidden;flex-wrap:wrap;gap:10px}
        .cat-container .cat-left{font-size:50px;transition:all .8s cubic-bezier(.34,1.56,.64,1);display:inline-block;cursor:pointer;position:relative;animation:catIdle 2s infinite ease-in-out;text-align:center}
        .cat-container .cat-left .action-text{font-size:12px;color:#d35400;font-weight:700;display:block;text-align:center;margin-top:2px}
        .cat-container .cat-left .fish{font-size:22px;display:inline-block;animation:fishSwim 1.5s infinite ease-in-out;position:absolute;right:-30px;top:10px}
        @keyframes fishSwim{0%,100%{transform:translateX(0) rotate(0)}50%{transform:translateX(10px) rotate(-5deg)}}
        @keyframes catIdle{0%,100%{transform:translateY(0) rotate(0)}50%{transform:translateY(-5px) rotate(-2deg)}}
        @keyframes catJump{0%{transform:translateY(0) scale(1)}30%{transform:translateY(-40px) scale(1.1) rotate(-5deg)}60%{transform:translateY(-40px) scale(1.1) rotate(5deg)}100%{transform:translateY(0) scale(1)}}
        @keyframes catCatch{0%{transform:scale(1)}25%{transform:scale(1.2) rotate(-10deg)}50%{transform:scale(1.3) rotate(10deg)}75%{transform:scale(1.2) rotate(-5deg)}100%{transform:scale(1) rotate(0)}}
        @keyframes catDance{0%,100%{transform:rotate(0) scale(1)}25%{transform:rotate(-10deg) scale(1.1)}75%{transform:rotate(10deg) scale(1.1)}}
        @keyframes fishCatch{0%{transform:translateX(0) scale(1);opacity:1}50%{transform:translateX(50px) scale(1.5);opacity:1}100%{transform:translateX(100px) scale(.5);opacity:0}}
        
        .cat-jump{animation:catJump .8s ease-in-out !important}
        .cat-catch{animation:catCatch .6s ease-in-out !important}
        .cat-dance{animation:catDance .8s ease-in-out !important}
        .cat-fish-catch{animation:fishCatch .8s ease-in-out forwards !important}
        
        .cat-container .cat-status{font-size:13px;color:#7f6000;font-weight:700;background:#fff3cd;padding:4px 14px;border-radius:20px;border:2px solid #f39c12;animation:pulseStatus 1.5s infinite}
        @keyframes pulseStatus{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.7;transform:scale(.97)}}
        .cat-container .cat-right{font-size:14px;color:#7f6000;text-align:right;line-height:1.4;min-width:150px}
        .cat-container .cat-right .highlight{color:#d35400;font-weight:700}
        .cat-container .progress-cat{flex:1;margin:0 15px;min-width:120px}
        .cat-container .progress-cat .mini-bar{height:6px;background:#e9ecef;border-radius:6px;overflow:hidden;margin-top:4px}
        .cat-container .progress-cat .mini-bar .mini-fill{height:100%;background:linear-gradient(90deg,#f39c12,#e67e22);border-radius:6px;transition:width .5s;width:0%}
        .cat-container .progress-cat .mini-label{font-size:11px;color:#666}
        
        .grid-2{display:grid;grid-template-columns:1fr 1fr;gap:15px;margin:10px 0}
        .upload-box{border:2px dashed #007bff;padding:15px;border-radius:8px;text-align:center;background:#f8f9fa;transition:all .3s;position:relative}
        .upload-box:hover{background:#e3f2fd;border-color:#0056b3}
        .upload-box.optional{border-color:#28a745}.upload-box.optional:hover{background:#d4edda}
        .upload-box .upload-cat-indicator{font-size:28px;display:block;margin-bottom:3px;animation:catIdle 2s infinite ease-in-out}
        .upload-box.active-cat{border-color:#f39c12;background:#fff8f0;box-shadow:0 0 20px rgba(243,156,18,.2)}
        .upload-box label{font-weight:700;display:block;margin-bottom:5px;font-size:14px}
        .upload-box .hint{font-size:11px;color:#666}
        .upload-box input[type=file]{padding:6px;font-size:12px;width:100%}
        
        .badge{display:inline-block;padding:2px 10px;border-radius:12px;font-size:10px;font-weight:700}
        .badge-required{background:#dc3545;color:#fff}
        .badge-optional{background:#28a745;color:#fff}
        .badge-txt{background:#17a2b8;color:#fff}
        .badge-excel{background:#217346;color:#fff}
        
        .flex{display:flex;gap:10px;flex-wrap:wrap;margin:10px 0}
        button{padding:8px 20px;border:none;border-radius:6px;cursor:pointer;font-size:14px;font-weight:700;transition:all .3s;position:relative}
        button:hover{transform:scale(1.03)}
        .btn-primary{background:#007bff;color:#fff}.btn-primary:hover{background:#0056b3}
        .btn-excel{background:#217346;color:#fff}.btn-excel:hover{background:#1a5c38}
        .btn-info{background:#17a2b8;color:#fff}.btn-info:hover{background:#138496}
        .btn-danger{background:#dc3545;color:#fff}.btn-danger:hover{background:#b02a37}
        .btn-warning{background:#ffc107;color:#333}.btn-warning:hover{background:#e0a800}
        .btn-sm{padding:4px 12px;font-size:12px}
        .btn .btn-icon{display:inline-block;margin-right:5px}
        
        .status-box{padding:8px 12px;border-radius:6px;font-size:13px;display:none;margin:5px 0}
        .status-box.success{background:#d4edda;color:#155724;border-left:4px solid #28a745}
        .status-box.info{background:#cce5ff;color:#004085;border-left:4px solid #007bff}
        .status-box.error{background:#f8d7da;color:#721c24;border-left:4px solid #dc3545}
        .status-box.warning{background:#fff3cd;color:#856404;border-left:4px solid #ffc107}
        
        .staff-preview{display:none;margin-top:8px;max-height:150px;overflow:auto;border:1px solid #ddd;border-radius:6px;padding:8px;font-size:11px;background:#fafafa}
        .preview{margin-top:15px;overflow:auto;max-height:400px;border:2px solid #ddd;border-radius:8px;display:none}
        table{width:100%;border-collapse:collapse;font-size:11px}
        th{background:#007bff;color:#fff;position:sticky;top:0;z-index:10;padding:6px;text-align:left;font-weight:700}
        td{padding:4px 6px;border-bottom:1px solid #eee;white-space:nowrap;max-width:120px;overflow:hidden;text-overflow:ellipsis}
        tr:nth-child(even){background:#f9f9f9}tr:hover{background:#e3f2fd}
        
        .stats{display:grid;grid-template-columns:repeat(auto-fit,minmax(100px,1fr));gap:8px;padding:10px;background:#e9ecef;border-radius:8px;margin:8px 0;font-size:13px}
        .stats .stat-item{text-align:center}
        .stats .stat-item .num{font-size:20px;font-weight:700;color:#007bff}
        .stats .stat-item .label{font-size:11px;color:#666}
        
        .progress-container{display:none;margin:10px 0}
        .progress-bar{height:22px;background:#e9ecef;border-radius:12px;overflow:hidden;position:relative}
        .progress-bar .fill{height:100%;background:linear-gradient(90deg,#f39c12,#e67e22);border-radius:12px;transition:width .3s;width:0%;text-align:center;color:#fff;font-size:12px;line-height:22px;font-weight:700}
        .progress-text{display:flex;justify-content:space-between;font-size:12px;color:#666;margin-top:3px}
        
        .advanced-toggle{cursor:pointer;color:#007bff;font-size:13px;user-select:none}
        .advanced-toggle:hover{text-decoration:underline}
        .advanced-content{display:none;padding:10px;background:#f8f9fa;border-radius:8px;margin:8px 0}
        .advanced-content.show{display:block}
        .row{display:flex;gap:10px;margin:6px 0;flex-wrap:wrap;align-items:center}
        .row label{font-weight:700;min-width:100px;font-size:13px}
        .row input{flex:1;padding:6px;border:2px solid #ddd;border-radius:5px;min-width:150px;font-size:13px}
        textarea{width:100%;height:80px;font-family:monospace;font-size:12px;padding:8px;border:2px solid #ddd;border-radius:6px;margin:5px 0;resize:vertical}
        textarea:focus{border-color:#007bff;outline:none}
        .text-small{font-size:12px;color:#666}
        
        .cat-footer{text-align:center;margin-top:15px;color:#b8860b;font-size:.85rem;opacity:.6;border-top:1px solid #f5e6ca;padding-top:12px}
        .cat-footer span{animation:meowText 2s infinite;display:inline-block}
        @keyframes meowText{0%,100%{transform:scale(1)}50%{transform:scale(1.05) rotate(-2deg);color:#e67e22}}
        
        .ignored-toggle{cursor:pointer;color:#856404;font-weight:700;font-size:12px}
        .ignored-toggle:hover{text-decoration:underline}
        .ignored-content{display:none;max-height:100px;overflow:auto}
        .ignored-content.show{display:block}
        
        /* HIDE CHECKBOXES - all forced ON */
        .checkbox-group{display:none !important}
        
        @media(max-width:768px){.grid-2{grid-template-columns:1fr}.cat-container{flex-wrap:wrap;justify-content:center;text-align:center;gap:10px}.cat-container .cat-right{text-align:center;width:100%}.cat-container .progress-cat{width:100%;margin:5px 0}.stats{grid-template-columns:repeat(2,1fr)}}
        @media(max-width:480px){.grid-2{grid-template-columns:1fr}.stats{grid-template-columns:1fr 1fr}.row{flex-direction:column;align-items:stretch}.row label{min-width:auto}}
    </style>
</head>
<body>
    <!-- Beach Paws Background -->
    <div class="paw-bg">🐾</div>
    <div class="paw-bg">🐾</div>
    <div class="paw-bg">🐾</div>
    <div class="paw-bg">🐾</div>
    <div class="paw-bg">🐾</div>
    <div class="paw-bg">🐾</div>

    <div class="container">
        <!-- Cat Header -->
        <div class="cat-header">
            <span class="cat-icon" onclick="playMeow()">🐱</span>
            <h1>🏖️ PAW Extractor <span style="font-size:0.6rem;color:#f39c12;">~ meow!</span></h1>
            <span class="sub">🌊🐾 healing di pantai</span>
        </div>

        <!-- Cat Adventure Bar -->
        <div class="cat-container" id="catContainer">
            <div class="cat-left" id="catAvatar">
                <span id="catEmoji">🐱</span>
                <span class="fish" id="catFish" style="display:none;">🐟</span>
                <span class="action-text" id="catActionText">📁 Upload Transaction</span>
            </div>
            <div class="progress-cat" id="catProgressArea" style="display:none;">
                <div class="mini-label" id="catProgressLabel">🐱 Processing... 0%</div>
                <div class="mini-bar">
                    <div class="mini-fill" id="catProgressFill"></div>
                </div>
            </div>
            <div class="cat-status" id="catStatus">😺 Sedia!</div>
            <div class="cat-right" id="catMessage">
                <span id="catSpeech">🐾 Upload transaction file dulu ya!</span>
            </div>
        </div>

        <!-- Status Messages -->
        <div id="errorMsg" class="status-box error"></div>
        <div id="successMsg" class="status-box success"></div>

        <!-- Upload Section -->
        <div class="grid-2">
            <div class="upload-box" id="uploadTransBox">
                <span class="upload-cat-indicator">🐱</span>
                <label>📁 Transaction <span class="badge badge-required">WAJIB</span></label>
                <input type="file" id="fileInput" accept="*/*" multiple>
                <div class="hint">Upload <strong>SEMUA</strong> jenis file (txt, csv, log, dat, pdf, xlsx, dll)</div>
                <div id="transStatus" class="status-box info" style="display:none;font-size:12px;">⏳ Loading...</div>
            </div>

            <div class="upload-box optional" id="uploadCashierBox">
                <span class="upload-cat-indicator">🐱</span>
                <label>📄 Cashier Listing <span class="badge badge-optional">OPTIONAL</span></label>
                <input type="file" id="cashierInput" accept=".txt,.csv,.xlsx,.xls">
                <div class="hint">Upload <span class="badge badge-txt">TXT</span> <span class="badge badge-excel">Excel</span> — auto match staff name</div>
                <div id="cashierStatus" class="status-box info" style="display:none;font-size:12px;">⏳ Loading...</div>
                <div id="staffCount" style="display:none;font-weight:bold;color:#28a745;font-size:13px;margin-top:5px;">✅ Staff loaded: <span id="staffTotal">0</span></div>
                <div id="staffPreview" class="staff-preview"></div>
            </div>
        </div>

        <!-- Paste Data -->
        <div class="row" style="margin-top:5px;">
            <label style="min-width:80px;">📝 Paste Data:</label>
            <textarea id="pasteInput" placeholder="Atau paste transaction data di sini..."></textarea>
        </div>

        <!-- Advanced Toggle -->
        <div style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:5px;">
            <span class="advanced-toggle" onclick="toggleAdvanced()">⚙️ Advanced Settings (widths, headers)</span>
            <span id="fileInfo" style="font-size:12px;color:#666;"></span>
        </div>

        <div class="advanced-content" id="advancedContent">
            <div class="row">
                <label>📏 Column Widths:</label>
                <input type="text" id="columnWidths" value="5,6,3,10,6,6,6,40,14,14,16,210,16,138">
            </div>
            <div class="row">
                <label>📋 Extract Columns:</label>
                <input type="text" id="extractColumns" value="0,1,2,3,4,6,8,10,12">
            </div>
            <div class="row">
                <label>📝 Header Names:</label>
                <input type="text" id="headerNames" value="#,storecode,date,pos,receipt,time,staff_id,staff_name,total_rm,bonuslink,parksoncard">
            </div>
            <div class="text-small">Total width: <span id="totalWidthDisplay">490</span></div>
        </div>

        <!-- Buttons -->
        <div class="flex">
            <button class="btn-primary" onclick="processAll()"><span class="btn-icon">🐱</span> Process All</button>
            <button class="btn-excel" onclick="exportExcel()"><span class="btn-icon">🐟</span> Export Excel</button>
            <button class="btn-info" onclick="showDashboard()">📊 Dashboard</button>
            <button class="btn-danger" onclick="clearAll()">🗑️ Clear</button>
            <button class="btn-warning btn-sm" onclick="loadSample()">📋 Sample</button>
        </div>

        <!-- Progress -->
        <div id="progressContainer" class="progress-container">
            <div class="progress-bar">
                <div class="fill" id="progressFill">0%</div>
            </div>
            <div class="progress-text">
                <span id="progressMsg">Starting...</span>
                <span id="progressETA">ETA: --</span>
            </div>
        </div>

        <!-- Stats -->
        <div class="stats" id="stats" style="display:none;">
            <div class="stat-item"><div class="num" id="rowCount">0</div><div class="label">Transactions</div></div>
            <div class="stat-item"><div class="num" id="staffMatchedCount">0</div><div class="label">Staff Matched</div></div>
            <div class="stat-item"><div class="num" id="dateCount">0</div><div class="label">Dates</div></div>
            <div class="stat-item"><div class="num" id="ignoredCount">0</div><div class="label">Ignored Lines</div></div>
        </div>

        <!-- Ignored Info -->
        <div id="ignoredInfo" style="display:none;background:#fff3cd;padding:8px;border-radius:6px;margin:8px 0;border-left:4px solid #ffc107;font-size:12px;">
            <div style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:5px;">
                <strong>🚫 Lines Ignored: <span id="ignoredCountDisplay">0</span></strong>
                <span class="ignored-toggle" onclick="toggleIgnored()">👁️ Toggle View</span>
            </div>
            <div class="ignored-content" id="ignoredContent">
                <div id="ignoredLines"></div>
            </div>
        </div>

        <!-- Dashboard -->
        <div id="dashboardContainer" style="display:none;background:#f8f9fa;padding:15px;border-radius:8px;margin:10px 0;border:2px solid #007bff;">
            <h2 style="font-size:16px;margin-bottom:10px;">📊 Staff Dashboard</h2>
            <div id="dashboardContent" style="font-size:13px;max-height:300px;overflow:auto;"></div>
        </div>

        <!-- Preview -->
        <div class="preview" id="previewContainer">
            <table id="resultTable">
                <thead id="tableHead"></thead>
                <tbody id="tableBody"></tbody>
            </table>
        </div>

        <div class="cat-footer">
            <span>🐾 @by Healing Pantai 🏖️🌊</span>
        </div>
    </div>

    <script>
        // ============================================================
        // PAW EXTRACTOR - FULL SCRIPT
        // ============================================================

        // SEMUA CHECKBOX WAJIB ON - hidden from UI
        var IGNORE_FIRST = true;
        var IGNORE_LAST = true;
        var SORT_BY_DATE = true;
        var AUTO_MATCH = true;

        var csvData = [];
        var headers = [];
        var extractIndices = [];
        var allRawData = [];
        var ignoredLines = [];
        var staffMap = {};
        var matchedCount = 0;
        var isProcessing = false;
        var transData = '';

        // ============================================================
        // CAT FUNCTIONS
        // ============================================================

        function updateCat(state, message, emoji, actionText) {
            var catEmoji = document.getElementById('catEmoji');
            var catStatus = document.getElementById('catStatus');
            var catSpeech = document.getElementById('catSpeech');
            var catActionText = document.getElementById('catActionText');
            var catFish = document.getElementById('catFish');
            var catAvatar = document.getElementById('catAvatar');

            if (catEmoji && emoji) catEmoji.textContent = emoji;
            if (catStatus) catStatus.textContent = state || '😺 Sedia!';
            if (catSpeech) catSpeech.textContent = message || '🐾 Tunggu arahan...';
            if (catActionText) catActionText.textContent = actionText || '📁 Upload Transaction';

            if (catAvatar) {
                catAvatar.classList.remove('cat-jump', 'cat-catch', 'cat-dance');
                if (state === 'jump') catAvatar.classList.add('cat-jump');
                else if (state === 'catch') {
                    catAvatar.classList.add('cat-catch');
                    if (catFish) {
                        catFish.style.display = 'inline-block';
                        catFish.classList.remove('cat-fish-catch');
                        setTimeout(function() { catFish.classList.add('cat-fish-catch'); }, 100);
                        setTimeout(function() { catFish.style.display = 'none';
                            catFish.classList.remove('cat-fish-catch'); }, 1000);
                    }
                } else if (state === 'dance') {
                    catAvatar.classList.add('cat-dance');
                }
            }

            var transBox = document.getElementById('uploadTransBox');
            var cashierBox = document.getElementById('uploadCashierBox');
            if (transBox) {
                transBox.classList.remove('active-cat');
                if (actionText && actionText.indexOf('Transaction') >= 0) transBox.classList.add('active-cat');
            }
            if (cashierBox) {
                cashierBox.classList.remove('active-cat');
                if (actionText && actionText.indexOf('Cashier') >= 0) cashierBox.classList.add('active-cat');
                if (actionText && actionText.indexOf('Export') >= 0) cashierBox.classList.remove('active-cat');
            }

            if (catStatus) {
                if (state === '😺 Sedia!' || state === '😺') {
                    catStatus.style.borderColor = '#28a745';
                    catStatus.style.background = '#d4edda';
                } else if (state === '🐱 Menunggu...' || state === '⏳') {
                    catStatus.style.borderColor = '#f39c12';
                    catStatus.style.background = '#fff3cd';
                } else if (state === '🎉 Selesai!' || state === '✅') {
                    catStatus.style.borderColor = '#28a745';
                    catStatus.style.background = '#d4edda';
                } else {
                    catStatus.style.borderColor = '#f39c12';
                    catStatus.style.background = '#fff3cd';
                }
            }
        }

        function showCatProgress(pct, label) {
            var area = document.getElementById('catProgressArea');
            if (area) area.style.display = 'block';
            var fill = document.getElementById('catProgressFill');
            if (fill) fill.style.width = pct + '%';
            var lbl = document.getElementById('catProgressLabel');
            if (lbl) lbl.textContent = '🐱 ' + (label || 'Processing...') + ' ' + pct + '%';
            updateCat('⏳', '🐾 Sedang proses... ' + pct + '%', '🐱', '⚡ Processing...');
        }

        function hideCatProgress() {
            var area = document.getElementById('catProgressArea');
            if (area) area.style.display = 'none';
        }

        function catJumpTo(elementId) {
            var el = document.getElementById(elementId);
            if (el) {
                el.classList.remove('active-cat');
                setTimeout(function() { el.classList.add('active-cat'); }, 50);
                el.scrollIntoView({ behavior: 'smooth', block: 'center' });
            }
            var avatar = document.getElementById('catAvatar');
            if (avatar) {
                avatar.classList.remove('cat-jump');
                setTimeout(function() { avatar.classList.add('cat-jump'); }, 50);
                setTimeout(function() { avatar.classList.remove('cat-jump'); }, 900);
            }
        }

        function catCatchFish() {
            var avatar = document.getElementById('catAvatar');
            var fish = document.getElementById('catFish');
            if (avatar) {
                avatar.classList.remove('cat-catch');
                setTimeout(function() { avatar.classList.add('cat-catch'); }, 50);
                setTimeout(function() { avatar.classList.remove('cat-catch'); }, 700);
            }
            if (fish) {
                fish.style.display = 'inline-block';
                fish.classList.remove('cat-fish-catch');
                setTimeout(function() { fish.classList.add('cat-fish-catch'); }, 150);
                setTimeout(function() { fish.style.display = 'none';
                    fish.classList.remove('cat-fish-catch'); }, 1000);
            }
            updateCat('catch', '🐟🐱 Ikan dapat!', '😺', '🎣 Menangkap ikan!');
            setTimeout(function() {
                updateCat('dance', '🎉🐱 Yeay!', '😸', '🎵 Menari gembira!');
            }, 500);
            setTimeout(function() {
                updateCat('😺 Sedia!', '🐾 Sedia untuk seterusnya!', '🐱', '📁 Upload Transaction');
            }, 1500);
        }

        function playMeow() {
            try {
                var ctx = new(window.AudioContext || window.webkitAudioContext)();
                var osc = ctx.createOscillator();
                var gain = ctx.createGain();
                osc.type = 'sawtooth';
                osc.frequency.setValueAtTime(800, ctx.currentTime);
                osc.frequency.linearRampToValueAtTime(1200, ctx.currentTime + 0.1);
                osc.frequency.linearRampToValueAtTime(600, ctx.currentTime + 0.2);
                gain.gain.setValueAtTime(0.25, ctx.currentTime);
                gain.gain.linearRampToValueAtTime(0.01, ctx.currentTime + 0.3);
                osc.connect(gain);
                gain.connect(ctx.destination);
                osc.start();
                osc.stop(ctx.currentTime + 0.3);
                if (ctx.state === 'suspended') ctx.resume();
            } catch (e) { /* ignore */ }
            var avatar = document.getElementById('catAvatar');
            if (avatar) {
                avatar.classList.remove('cat-jump');
                setTimeout(function() { avatar.classList.add('cat-jump'); }, 50);
                setTimeout(function() { avatar.classList.remove('cat-jump'); }, 800);
            }
        }

        // ============================================================
        // HELPERS
        // ============================================================

        function getEl(id) { var el = document.getElementById(id); if (!el) { console.warn('Element not found:', id); return null; } return el; }

        function showError(msg) { var el = getEl('errorMsg'); if (!el) return;
            el.textContent = '❌ ' + msg;
            el.style.display = 'block';
            setTimeout(function() { el.style.display = 'none'; }, 5000); }

        function showSuccess(msg) { var el = getEl('successMsg'); if (!el) return;
            el.textContent = '✅ ' + msg;
            el.style.display = 'block';
            setTimeout(function() { el.style.display = 'none'; }, 4000); }

        function updateProgress(pct, msg, eta) {
            var container = getEl('progressContainer');
            if (!container) return;
            container.style.display = 'block';
            var fill = getEl('progressFill');
            if (fill) { fill.style.width = pct + '%';
                fill.textContent = pct + '%'; }
            var pm = getEl('progressMsg');
            if (pm) pm.textContent = msg || 'Processing...';
            var pe = getEl('progressETA');
            if (pe) pe.textContent = eta ? 'ETA: ' + eta : 'ETA: --';
            showCatProgress(pct, msg);
        }

        function toggleAdvanced() { var el = getEl('advancedContent'); if (el) el.classList.toggle('show'); }

        function toggleIgnored() { var el = getEl('ignoredContent'); if (el) el.classList.toggle('show'); }

        function escapeHtml(str) { if (!str) return ''; var div = document.createElement('div');
            div.textContent = str; return div.innerHTML; }

        function roundPct(val) { return Math.round(val * 100) / 100; }

        // ============================================================
        // COLUMN WIDTHS
        // ============================================================

        var cw = getEl('columnWidths');
        if (cw) {
            cw.addEventListener('input', function() {
                var w = this.value.split(',').filter(function(s) { return s.trim() !== ''; }).map(Number);
                var total = w.reduce(function(a, b) { return a + b; }, 0);
                var td = getEl('totalWidthDisplay');
                if (td) td.textContent = total || 0;
            });
        }

        // ============================================================
        // CASHIER LISTING UPLOAD
        // ============================================================

        var ci = getEl('cashierInput');
        if (ci) {
            ci.addEventListener('change', function(e) {
                var file = this.files[0];
                if (!file) return;
                var status = getEl('cashierStatus');
                if (status) { status.style.display = 'block';
                    status.className = 'status-box info';
                    status.textContent = '⏳ Reading ' + file.name + '...'; }
                catJumpTo('uploadCashierBox');
                updateCat('🐱 Menunggu...', '🐾 Membaca file cashier...', '😺', '📄 Upload Cashier Listing');
                var ext = file.name.split('.').pop().toLowerCase();

                function processStaff(text) {
                    var result = extractStaffExcel1(text);
                    staffMap = result;
                    var count = Object.keys(staffMap).length;
                    var st = getEl('staffTotal');
                    if (st) st.textContent = count;
                    var sc = getEl('staffCount');
                    if (sc) sc.style.display = 'block';
                    var preview = getEl('staffPreview');
                    if (preview && count > 0) {
                        preview.style.display = 'block';
                        var html = '<strong>👥 Staff (' + count + '):</strong><br>';
                        var sorted = Object.keys(staffMap).sort();
                        for (var i = 0; i < Math.min(sorted.length, 15); i++) {
                            html += sorted[i] + ' → ' + staffMap[sorted[i]] + '<br>';
                        }
                        if (sorted.length > 15) html += '... and ' + (sorted.length - 15) + ' more';
                        preview.innerHTML = html;
                    }
                    if (status) { status.className = 'status-box success';
                        status.textContent = '✅ Loaded ' + count + ' staff!'; }
                    showSuccess('Cashier listing loaded: ' + count + ' staff');
                    catCatchFish();
                    updateCat('😺', '🐾 Staff loaded! ' + count + ' staff', '😸', '📄 Cashier Listing ✅');
                    setTimeout(function() {
                        catJumpTo('btnProcessAll');
                        updateCat('🐱 Menunggu...', '🐾 Sedia untuk Process All!', '🐱', '🚀 Process All');
                    }, 1500);
                }

                if (ext === 'txt' || ext === 'csv') {
                    var reader = new FileReader();
                    reader.onload = function(ev) { processStaff(ev.target.result); };
                    reader.readAsText(file);
                } else if (ext === 'xlsx' || ext === 'xls') {
                    var reader2 = new FileReader();
                    reader2.onload = function(ev) {
                        try {
                            var data = new Uint8Array(ev.target.result);
                            var workbook = XLSX.read(data, { type: 'array' });
                            var sheet = workbook.Sheets[workbook.SheetNames[0]];
                            var json = XLSX.utils.sheet_to_json(sheet, { header: 1 });
                            var lines = [];
                            for (var r = 0; r < json.length; r++) {
                                var row = json[r];
                                var line = '';
                                for (var c = 0; c < row.length; c++) {
                                    if (row[c]) line += row[c] + '\t';
                                }
                                if (line.trim()) lines.push(line.trim());
                            }
                            processStaff(lines.join('\n'));
                        } catch (err) {
                            if (status) { status.className = 'status-box error';
                                status.textContent = '❌ ' + err.message; }
                            showError('Error reading Excel: ' + err.message);
                        }
                    };
                    reader2.readAsArrayBuffer(file);
                } else {
                    if (status) { status.className = 'status-box error';
                        status.textContent = '❌ Unsupported format. Use TXT or Excel.'; }
                    showError('Unsupported format. Use TXT or Excel.');
                }
            });
        }

        // ============================================================
        // EXCEL1 EXTRACT STAFF
        // ============================================================

        function extractStaffExcel1(text) {
            var result = {};
            var statusList = ['SUPERVISOR', 'CASHIER', 'MANAGER', 'CHIEF_CSHR', 'TRAINEE', 'SENIOR', 'STAFF', 'CLERK'];
            var lines = text.split('\n');
            for (var i = 0; i < lines.length; i++) {
                var line = lines[i].trim();
                if (!line) continue;
                if (line.match(/^Cashier ID|^CASHIER ID|^STAFF ID|^ID|^Name|^NAME/i)) continue;
                var parts = line.split(/\t|\s{2,}/);
                var cleanParts = [];
                for (var p = 0; p < parts.length; p++) { if (parts[p].trim()) cleanParts.push(parts[p].trim()); }
                if (cleanParts.length < 2) continue;
                var id = cleanParts[0];
                if (!id.match(/^\d{6}$/)) continue;
                if (result[id]) continue;
                var firstWord = cleanParts[1] || '';
                if (firstWord.match(/^\d{1,2}\/\d{1,2}\/\d{2,4}$/)) continue;
                if (firstWord.match(/^(Yes|No)$/i)) continue;
                if (firstWord.match(/^\d+$/)) continue;
                if (firstWord.toUpperCase() === 'CASHIER') continue;
                if (firstWord.toUpperCase() === 'MANAGER') continue;
                if (firstWord.toUpperCase() === 'SUPERVISOR') continue;
                var foundStatus = null;
                var statusIndex = -1;
                var after = line.substring(id.length).trim();
                for (var s = 0; s < statusList.length; s++) {
                    var status = statusList[s];
                    var pos = after.toUpperCase().indexOf(status);
                    if (pos >= 0 && (statusIndex === -1 || pos < statusIndex)) { statusIndex = pos;
                        foundStatus = status; }
                }
                if (foundStatus) {
                    var namePart = after.substring(0, statusIndex).trim();
                    namePart = namePart.replace(/\d{6}/g, '').trim();
                    namePart = namePart.replace(/\d{1,2}\/\d{1,2}\/\d{2,4}/g, '').trim();
                    namePart = namePart.replace(/\d{1,2}:\d{2}:\d{2}/g, '').trim();
                    namePart = namePart.replace(/\b(Yes|No)\b/gi, '').trim();
                    namePart = namePart.replace(/\s+/g, ' ');
                    for (var s2 = 0; s2 < statusList.length; s2++) {
                        namePart = namePart.replace(new RegExp('\\b' + statusList[s2] + '\\b', 'gi'), '').trim();
                    }
                    if (!namePart || namePart.toUpperCase() === 'CASHIER' || namePart.toUpperCase() === 'MANAGER' ||
                        namePart.toUpperCase() === 'SUPERVISOR') continue;
                    if (namePart && namePart.length > 0 && namePart.match(/[A-Za-z\.\'\""\(\)\-]/)) {
                        result[id] = namePart;
                    }
                }
            }
            return result;
        }

        // ============================================================
        // TRANSACTION UPLOAD
        // ============================================================

        var fi = getEl('fileInput');
        if (fi) {
            fi.addEventListener('change', function(e) {
                var files = this.files;
                if (files.length === 0) return;
                catJumpTo('uploadTransBox');
                updateCat('🐱 Menunggu...', '🐾 Membaca ' + files.length + ' file...', '😺', '📁 Upload Transaction');
                var status = getEl('transStatus');
                if (status) { status.style.display = 'block';
                    status.className = 'status-box info';
                    status.textContent = '⏳ Loading ' + files.length + ' files...'; }
                var combined = '';
                var loaded = 0;
                for (var i = 0; i < files.length; i++) {
                    var reader = new FileReader();
                    reader.onload = function(ev) {
                        combined += ev.target.result + '\n';
                        loaded++;
                        if (loaded === files.length) {
                            transData = combined;
                            if (status) { status.className = 'status-box success';
                                status.textContent = '✅ ' + files.length + ' files loaded (' + combined.split('\n')
                                    .length + ' lines)'; }
                            var fi2 = getEl('fileInfo');
                            if (fi2) fi2.textContent = files.length + ' files | ' + combined.split('\n').length +
                                ' lines';
                            catCatchFish();
                            updateCat('😺', '🐾 ' + files.length + ' files loaded!', '😸', '📁 Transaction ✅');
                            setTimeout(function() {
                                catJumpTo('uploadCashierBox');
                                updateCat('🐱 Menunggu...', '🐾 Upload Cashier Listing (optional)', '🐱',
                                    '📄 Upload Cashier Listing');
                            }, 1500);
                            if (Object.keys(staffMap).length > 0 && AUTO_MATCH) {
                                setTimeout(function() {
                                    catJumpTo('btnProcessAll');
                                    updateCat('🐱 Menunggu...', '🐾 Sedia Process All!', '🐱',
                                        '🚀 Process All');
                                }, 2000);
                            }
                        }
                    };
                    reader.readAsText(files[i]);
                }
            });
        }

        // ============================================================
        // PASTE DATA
        // ============================================================

        var pasteInput = getEl('pasteInput');
        if (pasteInput) {
            pasteInput.addEventListener('input', function() {
                if (this.value.trim()) {
                    transData = this.value;
                    var fi = getEl('fileInfo');
                    if (fi) fi.textContent = 'Pasted: ' + this.value.split('\n').length + ' lines';
                    updateCat('😺', '🐾 Data pasted! ' + this.value.split('\n').length + ' lines', '😸',
                        '📝 Data pasted ✅');
                }
            });
        }

        // ============================================================
        // PROCESS ALL
        // ============================================================

        function processAll() {
            if (isProcessing) return;
            isProcessing = true;
            catJumpTo('btnProcessAll');
            updateCat('⏳', '🐾 Memproses data...', '🐱', '🚀 Processing...');
            var text = transData || '';
            if (!text.trim()) {
                showError('Please upload transaction files or paste data first!');
                isProcessing = false;
                updateCat('😺', '🐾 Upload transaction dulu ya!', '🐱', '📁 Upload Transaction');
                return;
            }
            var startTime = Date.now();
            var totalSteps = 5;
            var currentStep = 0;
            updateProgress(0, 'Starting...', '--');
            showCatProgress(0, 'Starting...');
            setTimeout(function() {
                try {
                    currentStep++;
                    var pct = Math.round((currentStep / totalSteps) * 100);
                    updateProgress(pct, 'Splitting data...', 'estimating...');
                    showCatProgress(pct, 'Splitting data...');
                    var result = splitDataInternal(text);
                    if (!result) { isProcessing = false;
                        updateCat('😺', '🐾 Error splitting data', '🐱', '❌ Error'); return; }
                    currentStep++;
                    pct = Math.round((currentStep / totalSteps) * 100);
                    updateProgress(pct, 'Matching staff names...', 'estimating...');
                    showCatProgress(pct, 'Matching staff...');
                    if (Object.keys(staffMap).length > 0 && AUTO_MATCH) {
                        matchStaffNames();
                    }
                    currentStep++;
                    pct = Math.round((currentStep / totalSteps) * 100);
                    updateProgress(pct, 'Updating display...', 'estimating...');
                    showCatProgress(pct, 'Updating display...');
                    displayTable();
                    var pc = getEl('previewContainer');
                    if (pc) pc.style.display = 'block';
                    var stats = getEl('stats');
                    if (stats) stats.style.display = 'grid';
                    currentStep++;
                    pct = Math.round((currentStep / totalSteps) * 100);
                    var elapsed = Math.round((Date.now() - startTime) / 1000);
                    updateProgress(pct, '✅ Complete!', elapsed + 's');
                    showCatProgress(100, '✅ Complete!');
                    var rc = getEl('rowCount');
                    if (rc) rc.textContent = csvData.length;
                    var smc = getEl('staffMatchedCount');
                    if (smc) smc.textContent = matchedCount;
                    var dates = {};
                    for (var i = 0; i < allRawData.length; i++) { if (allRawData[i].date) dates[allRawData[i].date] =
                            true; }
                    var dc = getEl('dateCount');
                    if (dc) dc.textContent = Object.keys(dates).length;
                    var msg = '✅ Extracted ' + csvData.length + ' rows';
                    if (matchedCount > 0) msg += ' | ' + matchedCount + ' staff matched';
                    showSuccess(msg);
                    catCatchFish();
                    updateCat('🎉', '🐾 Selesai! ' + csvData.length + ' transactions', '😸', '✅ Complete!');
                    setTimeout(function() {
                        catJumpTo('btnExportExcel');
                        updateCat('🐱', '🐾 Sedia Export Excel!', '😺', '📊 Export Excel');
                    }, 1500);
                    isProcessing = false;
                } catch (e) {
                    showError('Error: ' + e.message);
                    console.error(e);
                    isProcessing = false;
                    updateProgress(0, 'Error', '--');
                    updateCat('😺', '🐾 Error: ' + e.message, '🐱', '❌ Error');
                }
            }, 100);
        }

        // ============================================================
        // SPLIT DATA INTERNAL
        // ============================================================

        function splitDataInternal(text) {
            var widthStr = getEl('columnWidths');
            if (!widthStr) return false;
            var widths = widthStr.value.split(',').filter(function(w) { return w.trim() !== ''; }).map(Number);
            if (widths.length === 0 || widths.some(isNaN)) { showError('Invalid column widths!'); return false; }
            var ec = getEl('extractColumns');
            if (!ec) return false;
            var extractIndices2 = ec.value.split(',').filter(function(w) { return w.trim() !== ''; }).map(Number);
            if (extractIndices2.length === 0) { showError('Invalid extract columns!'); return false; }
            extractIndices = extractIndices2;
            var hn = getEl('headerNames');
            if (hn) { headers = hn.value.split(',').filter(function(w) { return w.trim() !== ''; }).map(function(w) { return w
                        .trim(); }); }
            if (headers.length !== 11) { headers = ['#', 'storecode', 'date', 'pos', 'receipt', 'time', 'staff_id',
                    'staff_name', 'total_rm', 'bonuslink', 'parksoncard'
                ]; }
            var lines = text.split('\n').filter(function(l) { return l.trim() !== ''; });
            ignoredLines = [];

            var startIdx = 0;
            if (IGNORE_FIRST && lines.length > 0) {
                if (isHeaderLine(lines[0])) { ignoredLines.push('Line 1: ' + lines[0].substring(0, 50) + '... [HEADER]');
                    startIdx = 1; }
            }

            var endIdx = lines.length;
            if (IGNORE_LAST && lines.length > 0) {
                if (isFooterLine(lines[lines.length - 1])) { ignoredLines.push('Line ' + lines.length + ': ' + lines[lines
                        .length - 1].substring(0, 50) + '... [FOOTER]');
                    endIdx = lines.length - 1; }
            }

            var dataLines = lines.slice(startIdx, endIdx);
            var filtered = [];
            for (var i = 0; i < dataLines.length; i++) {
                if (isHeaderLine(dataLines[i]) || isFooterLine(dataLines[i])) {
                    ignoredLines.push('Line ' + (startIdx + i + 1) + ': ' + dataLines[i].substring(0, 50) + '... [IGNORED]');
                } else { filtered.push(dataLines[i]); }
            }
            dataLines = filtered;
            var ii = getEl('ignoredInfo');
            var il = getEl('ignoredLines');
            var ic = getEl('ignoredCount');
            var icd = getEl('ignoredCountDisplay');
            if (ignoredLines.length > 0) {
                if (ii) ii.style.display = 'block';
                if (il) il.innerHTML = ignoredLines.map(function(l) { return '<div style="padding:2px 0;">🚫 ' + l +
                        '</div>'; }).join('');
                if (ic) ic.textContent = ignoredLines.length;
                if (icd) icd.textContent = ignoredLines.length;
            } else { if (ii) ii.style.display = 'none'; if (ic) ic.textContent = '0'; if (icd) icd.textContent = '0'; }
            if (dataLines.length === 0) { showError('No data found after removing header/footer!'); return false; }
            var totalWidth = widths.reduce(function(a, b) { return a + b; }, 0);
            var validLines = dataLines.filter(function(l) { return l.length === totalWidth; });
            if (validLines.length === 0) { showError('⚠️ No lines match width (' + totalWidth +
                    '). Check Column Widths.'); return false; }
            csvData = [];
            allRawData = [];
            var rowNum = 0;
            for (var li = 0; li < validLines.length; li++) {
                var line = validLines[li];
                var cols = [];
                var pos = 0;
                for (var w = 0; w < widths.length; w++) {
                    cols.push(line.substr(pos, widths[w]).trim());
                    pos += widths[w];
                }
                var extracted = extractIndices.map(function(idx) { return idx < cols.length ? cols[idx] : ''; });
                rowNum++;
                var storecode = extracted[0] || '';
                var dateRaw = extracted[1] || '';
                var dateFormatted = formatDate(dateRaw);
                var posVal = extracted[2] || '';
                var receipt = extracted[3] || '';
                var timeRaw = extracted[4] || '';
                var timeFormatted = formatTime(timeRaw);
                var staffId = extracted[5] || '';
                var totalRmRaw = extracted[6] || '';
                var bonuslink = extracted[7] || '';
                var parksoncard = extracted[8] || '';
                var totalRm = convertAmount(totalRmRaw);
                var row = [rowNum, storecode, dateFormatted, posVal, receipt, timeFormatted, staffId, '', totalRm,
                    bonuslink, parksoncard
                ];
                csvData.push(row);
                allRawData.push({
                    storecode: storecode,
                    date: dateFormatted,
                    pos: posVal,
                    receipt: receipt,
                    time: timeFormatted,
                    staffId: staffId,
                    staffName: '',
                    totalRm: totalRm,
                    bonuslink: bonuslink,
                    parksoncard: parksoncard,
                    hasBonus: bonuslink && bonuslink.length > 0,
                    hasParkson: parksoncard && parksoncard.length > 0
                });
            }
            if (SORT_BY_DATE) {
                var combined2 = csvData.map(function(r, idx) { return { row: r, date: r[2] }; });
                combined2.sort(function(a, b) { return parseDate(a.date) - parseDate(b.date); });
                csvData = combined2.map(function(item) { return item.row; });
                allRawData.sort(function(a, b) { return parseDate(a.date) - parseDate(b.date); });
            }
            return true;
        }

        // ============================================================
        // MATCH STAFF NAMES
        // ============================================================

        function matchStaffNames() {
            matchedCount = 0;
            var staffKeys = Object.keys(staffMap);
            if (staffKeys.length === 0) return;
            for (var i = 0; i < allRawData.length; i++) {
                var staffId = allRawData[i].staffId;
                var name = null;
                if (staffId) name = staffMap[staffId];
                if (!name && staffId) { var padded = String(staffId).trim(); while (padded.length < 6) padded = '0' +
                        padded;
                    name = staffMap[padded]; if (name) staffId = padded; }
                if (!name && staffId && staffId.length === 6) { var trimmed = String(parseInt(staffId));
                    name = staffMap[trimmed]; }
                if (name) { allRawData[i].staffName = name;
                    csvData[i][7] = name;
                    matchedCount++; } else { allRawData[i].staffName = '';
                    csvData[i][7] = ''; }
            }
            var smc = getEl('staffMatchedCount');
            if (smc) smc.textContent = matchedCount;
        }

        // ============================================================
        // UTILITY FUNCTIONS
        // ============================================================

        function convertAmount(a) {
            if (!a) return '';
            var m = a.match(/(\d+)\.(\d{2})([+-])/);
            if (m) { var num = parseInt(m[1]); var dec = m[2]; var sign = m[3] === '-' ? '-' : ''; return 'RM' + sign +
                    num + '.' + dec; }
            return a;
        }

        function formatTime(t) { if (!t || t.length < 6) return t; return t.substr(0, 2) + ':' + t.substr(2, 2) + ':' +
                t.substr(4, 2); }

        function formatDate(d) { if (!d || d.length < 6) return d; return d.substr(0, 2) + '-' + d.substr(2, 2) + '-20' +
                d.substr(4, 2); }

        function parseDate(d) { if (!d) return new Date(0); var parts = d.split('-'); if (parts.length !== 3) return new Date(
                0); return new Date(parseInt(parts[2]), parseInt(parts[1]) - 1, parseInt(parts[0])); }

        function isHeaderLine(l) { var t = l.trim(); if (!t) return false; if (t[0] === 'H') return true; if (t.match(
                /^\d{6}/) && t.length < 20) return true; if (t.length < 30 && t.match(/[A-Za-z]/)) return true; return false; }

        function isFooterLine(l) { var t = l.trim(); if (!t) return false; if (t[0] === 'F') return true; if (t.toUpperCase()
                .indexOf('TOTAL') === 0) return true; if (t.length < 20 && t.match(/^[A-Za-z0-9]+$/)) return true; return false; }

        // ============================================================
        // DISPLAY TABLE
        // ============================================================

        function displayTable() {
            var thead = getEl('tableHead');
            var tbody = getEl('tableBody');
            if (!thead || !tbody) return;
            var groups = {};
            for (var i = 0; i < csvData.length; i++) { var row = csvData[i]; var date = row[2] || 'Unknown'; if (!groups[
                    date]) groups[date] = [];
                groups[date].push(row); }
            var h = '<tr>';
            for (var hi = 0; hi < headers.length; hi++) { h += '<th style="background:#007bff;color:white;font-weight:bold;">' +
                    headers[hi] + '</th>'; }
            h += '</tr>';
            thead.innerHTML = h;
            var b = '';
            var dateKeys = Object.keys(groups).sort(function(a, b2) { return parseDate(a) - parseDate(b2); });
            var displayed = 0;
            for (var g = 0; g < dateKeys.length && displayed < 100; g++) {
                var date = dateKeys[g];
                var rows = groups[date];
                b += '<tr><td colspan="' + headers.length +
                    '" style="background:#fff3cd;font-weight:bold;padding:6px;">📅 ' + date + ' (' + rows.length +
                    ')</td></tr>';
                for (var r = 0; r < rows.length && displayed < 100; r++) {
                    var row = rows[r];
                    b += '<tr>';
                    for (var c = 0; c < row.length; c++) {
                        var val = row[c];
                        if (c === 7 && val) { b += '<td><span style="background:#d4edda;padding:1px 6px;border-radius:8px;font-size:10px;">✅ ' +
                                escapeHtml(val) + '</span></td>'; } else if (c === 9 && val) { b +=
                                '<td><span style="background:#ffc107;padding:1px 6px;border-radius:8px;font-size:10px;">🎯 ' +
                                escapeHtml(val) + '</span></td>'; } else if (c === 10 && val) { b +=
                                '<td><span style="background:#17a2b8;color:white;padding:1px 6px;border-radius:8px;font-size:10px;">🏷️ ' +
                                escapeHtml(val) + '</span></td>'; } else { b += '<td title="' + escapeHtml(val) +
                                '">' + escapeHtml(val) + '</td>'; }
                    }
                    b += '</tr>';
                    displayed++;
                }
            }
            if (csvData.length > 100) { b += '<tr><td colspan="' + headers.length +
                    '" style="text-align:center;color:#666;padding:10px;">... and ' + (csvData.length - 100) +
                    ' more</td></tr>'; }
            tbody.innerHTML = b;
        }

        // ============================================================
        // DASHBOARD
        // ============================================================

        function showDashboard() {
            if (csvData.length === 0) { showError('Please process data first!'); return; }
            var staffData = {};
            for (var i = 0; i < allRawData.length; i++) {
                var d = allRawData[i];
                var sid = d.staffId || 'Unknown';
                if (!staffData[sid]) { staffData[sid] = { total: 0, bonus: 0, parkson: 0, both: 0, name: d.staffName ||
                        '' }; }
                staffData[sid].total++;
                if (d.hasBonus) staffData[sid].bonus++;
                if (d.hasParkson) staffData[sid].parkson++;
                if (d.hasBonus && d.hasParkson) staffData[sid].both++;
                if (d.staffName) staffData[sid].name = d.staffName;
            }
            var html = '<p><strong>Total Staff:</strong> ' + Object.keys(staffData).length +
                ' | <strong>Transactions:</strong> ' + allRawData.length + '</p>';
            html +=
                '<table><tr><th style="background:#007bff;color:white;font-weight:bold;">#</th><th style="background:#007bff;color:white;font-weight:bold;">Staff ID</th><th style="background:#007bff;color:white;font-weight:bold;">Name</th><th style="background:#007bff;color:white;font-weight:bold;">Total</th><th style="background:#007bff;color:white;font-weight:bold;">Bonuslink</th><th style="background:#007bff;color:white;font-weight:bold;">Parkson</th><th style="background:#007bff;color:white;font-weight:bold;">Both</th><th style="background:#007bff;color:white;font-weight:bold;">% Parkson</th></tr>';
            var sorted = Object.keys(staffData).sort(function(a, b) { return staffData[b].total - staffData[a].total; });
            var rank = 0;
            for (var s = 0; s < sorted.length; s++) {
                var d2 = staffData[sorted[s]];
                rank++;
                var pct = d2.total > 0 ? roundPct((d2.parkson / d2.total) * 100) : 0;
                html += '<tr><td>' + rank + '</td><td>' + sorted[s] + '</td><td>' + escapeHtml(d2.name) + '</td><td>' +
                    d2.total + '</td><td>' + d2.bonus + '</td><td>' + d2.parkson + '</td><td>' + d2.both +
                    '</td><td>' + pct + '%</td></tr>';
            }
            html += '</table>';
            var totalBonus = 0,
                totalParkson = 0,
                totalBoth = 0;
            for (var s2 = 0; s2 < sorted.length; s2++) {
                var d3 = staffData[sorted[s2]];
                totalBonus += d3.bonus;
                totalParkson += d3.parkson;
                totalBoth += d3.both;
            }
            var overallPct = allRawData.length > 0 ? roundPct((totalParkson / allRawData.length) * 100) : 0;
            html += '<div style="margin-top:10px;padding:8px;background:#e9ecef;border-radius:6px;font-weight:bold;">';
            html += '📊 Total: ' + allRawData.length + ' | Bonuslink: ' + totalBonus + ' | Parkson: ' + totalParkson +
                ' | Both: ' + totalBoth + ' | % Parkson: ' + overallPct + '%';
            html += '</div>';
            var dc = getEl('dashboardContent');
            if (dc) dc.innerHTML = html;
            var dc2 = getEl('dashboardContainer');
            if (dc2) dc2.style.display = 'block';
            showSuccess('📊 Dashboard generated! 🏖️');
            catCatchFish();
            updateCat('🎉', '🐾 Dashboard siap!', '😸', '📊 Dashboard ✅');
        }

        // ============================================================
        // EXPORT EXCEL - DENGAN HEADER & DATA BETUL
        // ============================================================

        function exportExcel() {
            if (csvData.length === 0) { showError('No data to export! Process first.'); return; }
            catJumpTo('btnExportExcel');
            updateCat('⏳', '🐾 Menyediakan Excel...', '🐱', '📊 Exporting...');
            if (Object.keys(staffMap).length > 0) matchStaffNames();

            try {
                // ============================================================
                // RULE: Exclude POS '001' dari semua sheet EXCEPT Raw Data
                // ============================================================
                var filteredData = allRawData.filter(function(d) { return d.pos !== '001'; });

                var wb = XLSX.utils.book_new();

                // ==========================================
                // SHEET 1: Staff Dashboard
                // ==========================================
                var staffData = {};
                for (var i = 0; i < filteredData.length; i++) {
                    var d = filteredData[i];
                    var sid = d.staffId || 'Unknown';
                    if (!staffData[sid]) { staffData[sid] = { total: 0, parkson: 0, bonus: 0, both: 0, name: d.staffName || '' }; }
                    staffData[sid].total++;
                    if (d.hasParkson) staffData[sid].parkson++;
                    if (d.hasBonus) staffData[sid].bonus++;
                    if (d.hasBonus && d.hasParkson) staffData[sid].both++;
                    if (d.staffName) staffData[sid].name = d.staffName;
                }
                
                var staffRows = [
                    ['#', 'Staff ID', 'Staff Name', 'Total', 'Parkson', 'Bonuslink', 'Both', '% Parkson', '% Bonuslink']
                ];
                var sorted = Object.keys(staffData).sort(function(a, b) { return staffData[b].total - staffData[a].total; });
                var rank = 0;
                for (var s = 0; s < sorted.length; s++) {
                    var d2 = staffData[sorted[s]];
                    rank++;
                    var pctParkson = d2.total > 0 ? roundPct((d2.parkson / d2.total) * 100) : 0;
                    var pctBonus = d2.total > 0 ? roundPct((d2.bonus / d2.total) * 100) : 0;
                    staffRows.push([rank, sorted[s], d2.name, d2.total, d2.parkson, d2.bonus, d2.both, pctParkson + '%', pctBonus + '%']);
                }
                var totalParkson = 0, totalBonus = 0, totalBoth = 0;
                for (var s2 = 0; s2 < sorted.length; s2++) {
                    var d3 = staffData[sorted[s2]];
                    totalParkson += d3.parkson;
                    totalBonus += d3.bonus;
                    totalBoth += d3.both;
                }
                var overallPctParkson = filteredData.length > 0 ? roundPct((totalParkson / filteredData.length) * 100) : 0;
                var overallPctBonus = filteredData.length > 0 ? roundPct((totalBonus / filteredData.length) * 100) : 0;
                staffRows.push(['TOTAL', '', '', filteredData.length, totalParkson, totalBonus, totalBoth, overallPctParkson + '%', overallPctBonus + '%']);

                var ws1 = XLSX.utils.aoa_to_sheet(staffRows);
                ws1['!cols'] = [{ wch: 6 }, { wch: 15 }, { wch: 25 }, { wch: 10 }, { wch: 12 }, { wch: 12 }, { wch: 10 }, { wch: 12 }, { wch: 12 }];
                XLSX.utils.book_append_sheet(wb, ws1, 'Staff Dashboard');

                // ==========================================
                // SHEET 2: SWAP Parkson
                // ==========================================
                var posData = {};
                for (var i2 = 0; i2 < filteredData.length; i2++) {
                    var d4 = filteredData[i2];
                    var pos = d4.pos || 'Unknown';
                    if (!posData[pos]) { posData[pos] = { total: 0, swap: 0, noSwap: 0, dates: [] }; }
                    posData[pos].total++;
                    if (d4.hasParkson) posData[pos].swap++;
                    else posData[pos].noSwap++;
                    if (d4.date && posData[pos].dates.indexOf(d4.date) === -1) posData[pos].dates.push(d4.date);
                }
                var posKeys = Object.keys(posData);
                
                var swapRows = [['RANK', 'POS', 'RANGE DATE', 'TOTAL (SWAP PC)']];
                var swapList = [];
                for (var p = 0; p < posKeys.length; p++) {
                    var d5 = posData[posKeys[p]];
                    if (d5.swap > 0) {
                        var range = d5.dates.length > 0 ? d5.dates.sort()[0] + ' - ' + d5.dates.sort()[d5.dates.length - 1] : '';
                        swapList.push({ pos: posKeys[p], count: d5.swap, range: range });
                    }
                }
                swapList.sort(function(a, b) { return b.count - a.count; });
                var rank2 = 0;
                for (var s3 = 0; s3 < swapList.length; s3++) { rank2++;
                    swapRows.push([rank2, swapList[s3].pos, swapList[s3].range, swapList[s3].count]); }
                var totalSwap = 0;
                for (var s4 = 0; s4 < swapList.length; s4++) totalSwap += swapList[s4].count;
                swapRows.push(['TOTAL', '', '', totalSwap]);
                var ws2 = XLSX.utils.aoa_to_sheet(swapRows);
                ws2['!cols'] = [{ wch: 8 }, { wch: 15 }, { wch: 25 }, { wch: 12 }];
                XLSX.utils.book_append_sheet(wb, ws2, 'SWAP Parkson');

                // ==========================================
                // SHEET 3: NOT SWAP
                // ==========================================
                var noSwapRows = [['RANK', 'POS', 'RANGE DATE', 'TOTAL (NOT SWAP PC)']];
                var noSwapList = [];
                for (var p2 = 0; p2 < posKeys.length; p2++) {
                    var d6 = posData[posKeys[p2]];
                    if (d6.noSwap > 0) {
                        var range2 = d6.dates.length > 0 ? d6.dates.sort()[0] + ' - ' + d6.dates.sort()[d6.dates.length - 1] : '';
                        noSwapList.push({ pos: posKeys[p2], count: d6.noSwap, range: range2 });
                    }
                }
                noSwapList.sort(function(a, b) { return b.count - a.count; });
                var rank3 = 0;
                for (var s5 = 0; s5 < noSwapList.length; s5++) { rank3++;
                    noSwapRows.push([rank3, noSwapList[s5].pos, noSwapList[s5].range, noSwapList[s5].count]); }
                var totalNoSwap = 0;
                for (var s6 = 0; s6 < noSwapList.length; s6++) totalNoSwap += noSwapList[s6].count;
                noSwapRows.push(['TOTAL', '', '', totalNoSwap]);
                var ws3 = XLSX.utils.aoa_to_sheet(noSwapRows);
                ws3['!cols'] = [{ wch: 8 }, { wch: 15 }, { wch: 25 }, { wch: 12 }];
                XLSX.utils.book_append_sheet(wb, ws3, 'NOT SWAP Parkson');

                // ==========================================
                // SHEET 4: POS Tabulation
                // ==========================================
                var tabRows = [
                    ['RANK', 'POS', 'RANGE DATE', 'TOTAL', 'PARKSON', 'BONUSLINK', 'BOTH', '% PARKSON', '% BONUSLINK']
                ];
                var tabList = [];
                for (var p3 = 0; p3 < posKeys.length; p3++) {
                    var d7 = posData[posKeys[p3]];
                    var range3 = d7.dates.length > 0 ? d7.dates.sort()[0] + ' - ' + d7.dates.sort()[d7.dates.length - 1] : '';
                    var parksonCount = d7.swap;
                    var bonusCount = 0;
                    for (var i3 = 0; i3 < filteredData.length; i3++) {
                        if (filteredData[i3].pos === posKeys[p3] && filteredData[i3].hasBonus) bonusCount++;
                    }
                    var bothCount = 0;
                    for (var i4 = 0; i4 < filteredData.length; i4++) {
                        if (filteredData[i4].pos === posKeys[p3] && filteredData[i4].hasBonus && filteredData[i4].hasParkson) bothCount++;
                    }
                    var pctParksonPos = d7.total > 0 ? roundPct((parksonCount / d7.total) * 100) : 0;
                    var pctBonusPos = d7.total > 0 ? roundPct((bonusCount / d7.total) * 100) : 0;
                    tabList.push({
                        pos: posKeys[p3],
                        total: d7.total,
                        parkson: parksonCount,
                        bonus: bonusCount,
                        both: bothCount,
                        pctParkson: pctParksonPos,
                        pctBonus: pctBonusPos,
                        range: range3
                    });
                }
                tabList.sort(function(a, b) { return b.total - a.total; });
                var rank4 = 0;
                for (var s7 = 0; s7 < tabList.length; s7++) {
                    rank4++;
                    tabRows.push([rank4, tabList[s7].pos, tabList[s7].range, tabList[s7].total, tabList[s7].parkson, tabList[s7].bonus, tabList[s7].both, tabList[s7].pctParkson + '%', tabList[s7].pctBonus + '%']);
                }
                var totalAll = 0, totalParksonAll2 = 0, totalBonusAll = 0, totalBothAll = 0;
                for (var s8 = 0; s8 < tabList.length; s8++) {
                    totalAll += tabList[s8].total;
                    totalParksonAll2 += tabList[s8].parkson;
                    totalBonusAll += tabList[s8].bonus;
                    totalBothAll += tabList[s8].both;
                }
                var overallPctParksonPos = totalAll > 0 ? roundPct((totalParksonAll2 / totalAll) * 100) : 0;
                var overallPctBonusPos = totalAll > 0 ? roundPct((totalBonusAll / totalAll) * 100) : 0;
                tabRows.push(['TOTAL', '', '', totalAll, totalParksonAll2, totalBonusAll, totalBothAll, overallPctParksonPos + '%', overallPctBonusPos + '%']);
                var ws4 = XLSX.utils.aoa_to_sheet(tabRows);
                ws4['!cols'] = [{ wch: 8 }, { wch: 15 }, { wch: 25 }, { wch: 10 }, { wch: 12 }, { wch: 12 }, { wch: 10 }, { wch: 12 }, { wch: 12 }];
                XLSX.utils.book_append_sheet(wb, ws4, 'POS Tabulation');

                // ==========================================
                // SHEET 5: Raw Data (INCLUDE SEMUA POS)
                // ==========================================
                var rawRows = [headers];
                for (var r = 0; r < csvData.length; r++) {
                    rawRows.push(csvData[r]);
                }
                var ws5 = XLSX.utils.aoa_to_sheet(rawRows);
                ws5['!cols'] = headers.map(function(h, idx) {
                    var col = { wch: 14 };
                    if (idx === 10) { col.numFmt = '@'; }
                    return col;
                });
                XLSX.utils.book_append_sheet(wb, ws5, 'Raw Data');

                // ==========================================
                // SHEET 6: POS by Cashier by Date
                // ==========================================
                var posCashierDate = {};
                for (var i5 = 0; i5 < filteredData.length; i5++) {
                    var d8 = filteredData[i5];
                    var key = d8.date + '|' + d8.pos + '|' + d8.staffId;
                    if (!posCashierDate[key]) {
                        posCashierDate[key] = {
                            date: d8.date,
                            pos: d8.pos,
                            staffId: d8.staffId,
                            staffName: d8.staffName || '',
                            total: 0,
                            swap: 0,
                            noSwap: 0,
                            bonus: 0
                        };
                    }
                    posCashierDate[key].total++;
                    if (d8.hasParkson) posCashierDate[key].swap++;
                    else posCashierDate[key].noSwap++;
                    if (d8.hasBonus) posCashierDate[key].bonus++;
                    if (d8.staffName) posCashierDate[key].staffName = d8.staffName;
                }

                var pcdRows = [
                    ['DATE', 'POS', 'STAFF ID', 'STAFF NAME', 'TOTAL TRANS', 'SWAP (PC)', 'NOT SWAP (PC)', 'BONUSLINK', '% SWAP (PC)', '% BONUSLINK']
                ];
                var pcdList = Object.keys(posCashierDate).map(function(k) { return posCashierDate[k]; });
                pcdList.sort(function(a, b) {
                    var dateCompare = a.date.localeCompare(b.date);
                    if (dateCompare !== 0) return dateCompare;
                    var posCompare = a.pos.localeCompare(b.pos);
                    if (posCompare !== 0) return posCompare;
                    return a.staffId.localeCompare(b.staffId);
                });

                for (var s9 = 0; s9 < pcdList.length; s9++) {
                    var d9 = pcdList[s9];
                    var pctSwapPcd = d9.total > 0 ? roundPct((d9.swap / d9.total) * 100) : 0;
                    var pctBonusPcd = d9.total > 0 ? roundPct((d9.bonus / d9.total) * 100) : 0;
                    pcdRows.push([d9.date, d9.pos, d9.staffId, d9.staffName, d9.total, d9.swap, d9.noSwap, d9.bonus, pctSwapPcd + '%', pctBonusPcd + '%']);
                }
                var totalTransPcd = 0, totalSwapPcd = 0, totalNoSwapPcd = 0, totalBonusPcd = 0;
                for (var s10 = 0; s10 < pcdList.length; s10++) {
                    totalTransPcd += pcdList[s10].total;
                    totalSwapPcd += pcdList[s10].swap;
                    totalNoSwapPcd += pcdList[s10].noSwap;
                    totalBonusPcd += pcdList[s10].bonus;
                }
                var overallPctSwapPcd = totalTransPcd > 0 ? roundPct((totalSwapPcd / totalTransPcd) * 100) : 0;
                var overallPctBonusPcd = totalTransPcd > 0 ? roundPct((totalBonusPcd / totalTransPcd) * 100) : 0;
                pcdRows.push(['TOTAL', '', '', '', totalTransPcd, totalSwapPcd, totalNoSwapPcd, totalBonusPcd, overallPctSwapPcd + '%', overallPctBonusPcd + '%']);

                var ws6 = XLSX.utils.aoa_to_sheet(pcdRows);
                ws6['!cols'] = [{ wch: 14 }, { wch: 10 }, { wch: 15 }, { wch: 25 }, { wch: 12 }, { wch: 12 }, { wch: 12 }, { wch: 12 }, { wch: 12 }, { wch: 12 }];
                XLSX.utils.book_append_sheet(wb, ws6, 'POS by Cashier by Date');

                var now = new Date();
                var dateStr = now.getFullYear() + '-' + String(now.getMonth() + 1).padStart(2, '0') + '-' + String(now.getDate()).padStart(2, '0');
                var filename = 'POS_Report_' + dateStr + '.xlsx';

                XLSX.writeFile(wb, filename);

                catCatchFish();
                updateCat('🎉', '🐾 Excel exported! 6 sheets', '😸', '📊 Export Excel ✅');
                showSuccess('📊 Excel exported: ' + filename + ' (6 sheets) 🏖️🐱');

            } catch (e) {
                showError('Export error: ' + e.message);
                console.error(e);
                updateCat('😺', '🐾 Error exporting: ' + e.message, '🐱', '❌ Error');
            }
        }

        // ============================================================
        // SAMPLE DATA
        // ============================================================

        function loadSample() {
            var sample =
                'H21072612722072612:05:23  V002\n00127210726101000042787610392810401625800500111100000000020000000634.00+00000000010000000605.00+00000         6018840020120750000000.00+00000302.00+00000303.00+000000.00+00000605.00+                                                    00000000.00+00000000.00+                               0000000000.00+PMY LS6.2 20141230  00000605.00+0201000002131051                 430871    2                 0000000000.00+00000000.00+                                                                    \n00127210726101000042787710544410554025800500111100000000030000000673.00+00000000010000000039.00+00000         6018840016815264000000.00+00000019.00+00000000.00+000000.00+00000019.00+                                                    00000000.00+00000000.00+                               0000000000.00+PMY LS6.2 20141230  00000039.00+0201000000371930                 430872    2                 0000000000.00+00000000.00+                                                                    \nF000596';
            transData = sample;
            var pi = getEl('pasteInput');
            if (pi) pi.value = sample;
            var fi = getEl('fileInfo');
            if (fi) fi.textContent = 'Sample: 3 transactions';
            var ts = getEl('transStatus');
            if (ts) { ts.style.display = 'block';
                ts.className = 'status-box success';
                ts.textContent = '✅ Sample data loaded (3 transactions)'; }
            showSuccess('Sample loaded! Click Process All. 🏖️');
            updateCat('😺', '🐾 Sample data loaded!', '😸', '📋 Sample ✅');
            catCatchFish();
        }

        // ============================================================
        // CLEAR ALL
        // ============================================================

        function clearAll() {
            transData = '';
            csvData = [];
            allRawData = [];
            staffMap = {};
            matchedCount = 0;
            ignoredLines = [];
            var fi = getEl('fileInput');
            if (fi) fi.value = '';
            var ci = getEl('cashierInput');
            if (ci) ci.value = '';
            var pi = getEl('pasteInput');
            if (pi) pi.value = '';
            var pc = getEl('previewContainer');
            if (pc) pc.style.display = 'none';
            var dc = getEl('dashboardContainer');
            if (dc) dc.style.display = 'none';
            var stats = getEl('stats');
            if (stats) stats.style.display = 'none';
            var sc = getEl('staffCount');
            if (sc) sc.style.display = 'none';
            var sp = getEl('staffPreview');
            if (sp) sp.style.display = 'none';
            var cs = getEl('cashierStatus');
            if (cs) cs.style.display = 'none';
            var ts = getEl('transStatus');
            if (ts) ts.style.display = 'none';
            var pg = getEl('progressContainer');
            if (pg) pg.style.display = 'none';
            var ii = getEl('ignoredInfo');
            if (ii) ii.style.display = 'none';
            var fii = getEl('fileInfo');
            if (fii) fii.textContent = '';
            hideCatProgress();
            updateCat('😺 Sedia!', '🐾 Sedia untuk seterusnya!', '🐱', '📁 Upload Transaction');
            showSuccess('Cleared! 🌊');
        }

        // ============================================================
        // INIT
        // ============================================================

        updateCat('😺 Sedia!', '🐾 Upload transaction file dulu ya!', '🐱', '📁 Upload Transaction');
        console.log('🐱🏖️ PAW Extractor ready!');
        console.log('📁 Upload transaction files + Cashier Listing (optional)');
        console.log('🐱 Kucing akan melompat dan menangkap ikan!');
    </script>
</body>
</html>
